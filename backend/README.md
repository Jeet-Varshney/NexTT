# NexT Authentication API

This directory is reserved for the backend services, RESTful routes, and database models using Node.js/Express, Python, or your preferred backend stack.

## Architecture

- Intended to run on a separate port during local development.
- Will handle all critical logic securely, decoupled from the frontend presentation layer.
